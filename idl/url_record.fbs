include "direct_url.fbs";
include "private_url.fbs";

namespace ec_prv;

union URLData {
	PrivateURL,
	DirectURL
}

table URLRecord {
	version:uint8 = 1;
	expiry:uint64;
	data:URLData;
}

root_type URLRecord;
