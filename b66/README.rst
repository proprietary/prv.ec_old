==================================
base-66 url-safe encoding/decoding
==================================

Like base64, but optimized for URLs. It uses all 66 non-reserved
characters allowed in URLs (see: RFC 3986). Two extra characters ('~',
'.') are added to the standard url-safe base64 alphabet.
